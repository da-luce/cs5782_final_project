# Results

This directory contains JSON result files produced by `code/train.py` for each experiment.

Each file is named `{mode}_{dataset}.json` (e.g. `baseline_sst2.json`) and records accuracy, training time, parameter counts, and hyperparameters for that run.

Result files are committed here after experiments are run in Colab.
